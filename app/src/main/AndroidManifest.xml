<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lit.xiaomei">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme"
        tools:replace="android:icon,android:theme,android:allowBackup ">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EditReleaseMsgActivity"
            android:label="@string/title_activity_edit_release_msg" />
        <activity
            android:name=".InformationDetailsActivity"
            android:label="@string/title_activity_information_details" />
        <activity
            android:name=".RegistActivity"
            android:label="@string/title_activity_regist" />
        <activity
            android:name=".MyIncomeInformationActivity"
            android:label="@string/title_activity_my_income_information"></activity>
    </application>

</manifest>